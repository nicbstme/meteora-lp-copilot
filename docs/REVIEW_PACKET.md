# Review Packet

## Local artifact

- App: Meteora LP Copilot
- Bounty target: LPAgent.io | API integrate Sidetrack
- Local URL: <http://127.0.0.1:5173/>
- Production preview URL: <http://127.0.0.1:4173/>
- Screenshot: [`docs/meteora-lp-copilot-demo.png`](./meteora-lp-copilot-demo.png)

## Requirement mapping

| Bounty requirement | Evidence in this repo |
| --- | --- |
| Project must use one or more LP Agent endpoints | `src/App.tsx` calls `GET /pools/discover`, `POST /position/decrease-quotes`, and `POST /position/decrease-tx`. |
| Project must use Zap In or Zap Out API | `src/App.tsx` includes `POST /pools/{poolId}/add-tx` for Zap In and Zap Out quote/transaction flows. |
| Submission must include clear demo of LP Agent usage | `SUBMISSION.md` has the demo script; `docs/meteora-lp-copilot-demo.png` shows the dry-run payload behavior. |
| Project GitHub link | Blocked until repo is published externally. |
| Project website | Blocked until app is deployed externally. |
| Loom/demo video | Demo script exists; recording/uploading is external and still blocked on approval. |
| Agent API payload | `submission.payload.json` contains the local payload generated by `npm run agent:write-payload`. |

## Verification performed

```bash
npm run agent:payload
npm run agent:write-payload
npm run lint
npm run build
npm run verify
npm run wallets:check
npm run preflight
```

The artifact commands passed locally after the helper scripts were added.
`npm run wallets:check` is the direct success-condition check and is expected
to report `successConditionMet: false` until a bounty or other payment arrives.

Production preview was also checked locally with the browser title
`Meteora LP Copilot` and the main controls visible.

GitHub Actions workflow coverage is defined in
`.github/workflows/ci.yml` for install, verify, lint, build, and payload
generation after publication. The same sequence was validated locally with:

```bash
npm ci && npm run verify && npm run lint && npm run build && npm run agent:write-payload
```

## Known blockers

- Superteam submission requires publishing project links externally.
- LP Agent live calls require an API key from the LP Agent portal.
- Prize payout requires Superteam's human claim/payout flow if using the agent API.
- Current wallet balance checks showed zero value, so the goal success condition is not met yet.
- The exact external approval gates are listed in `EXTERNAL_ACTIONS.md`.
